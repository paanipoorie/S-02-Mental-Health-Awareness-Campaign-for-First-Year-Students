---
import Navbar from '../components/nav/Navbar.astro';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  currentRole?: 'STUDENT' | 'MENTOR' | 'ADMIN' | null;
  displayName?: string;
  isVerified?: boolean;
}

const {
  title = 'Campus Anonymous Peer Support',
  description = 'A safe, confidential anonymous peer support platform for first-year students.',
  currentRole = null,
  displayName = '',
  isVerified = false,
} = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Geist:wght@100..900&family=Geist+Mono:wght@100..900&display=swap"
      rel="stylesheet"
    />
  </head>
  <body
    class="flex min-h-screen flex-col bg-background-100 font-sans text-primary antialiased selection:bg-blue-100 selection:text-blue-900"
  >
    <Navbar currentRole={currentRole} displayName={displayName} isVerified={isVerified} />

    <main class="mx-auto w-full max-w-7xl flex-1 px-4 py-8 sm:px-6 lg:px-8">
      <slot />
    </main>

    <footer
      class="border-t border-gray-200 bg-background-200 py-6 text-center text-xs text-gray-500"
    >
      <div
        class="mx-auto flex max-w-7xl flex-col items-center justify-between gap-4 px-4 sm:flex-row"
      >
        <span>© 2026 Campus Peer Support. Strictly Confidential & Safe Space.</span>
        <div class="flex items-center gap-4 text-gray-500">
          <a href="/resources" class="transition-colors hover:text-gray-900">Helplines</a>
          <a href="/privacy" class="transition-colors hover:text-gray-900">Privacy Guarantee</a>
        </div>
      </div>
    </footer>
  </body>
</html>
